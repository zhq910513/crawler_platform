# 1.0.67 平台自检需确认状态一次性收敛专项

## 核心目标

本版本一次性收敛运行总览平台自检的“需确认”状态体验，避免平台侧已经没有必须处理项时仍像部署失败，并把执行节点验证动作做成可直接复制执行的闭环。

## 主要改动

- 顶部自检状态改成分组胶囊，避免“需确认必须处理 0需确认 4”挤压。
- 需确认状态图标由问号调整为信息提示语义。
- 需确认状态直接展示执行节点验证脚本预览，并提供复制按钮。
- 当前重点事项最多展示 6 项，并在超出时提示还有剩余项。
- 历史快照展示时统一把旧“阻断/阻断项”归一为“必须处理/必须处理项”。
- 后端使用部署阶段已记录的执行组件镜像校验值降低镜像版本和校验值误报。
- 清理运行总览自检文案中的“Agent 容器”等偏内部表达。
- 增加契约检查，防止需确认状态 UI、验证脚本和历史文案再次回退。
