# crawler_platform 1.0.41

## 前端体验

- 恢复旧版扁平导航视觉，不再在侧边栏展示导航分组标题。
- 收紧左侧导航、顶部栏和页面内容间距，恢复更清爽的商业化布局。
- 项目发布页优化为更聚焦的发布表单 + 准备度卡片。
- 发布助手调整为右侧常驻侧栏，流程未完成前只能收起为可拖动圆形浮窗。
- 新增服务器流程不再展示或编辑控制端公网回调地址；该地址只由系统设置统一管理。

## 校验重点

- 保持强流水线阻断规则：发布条件不满足时不能继续下游步骤。
- 保持服务器接入命令由后端基于系统设置生成，不由新增服务器表单手工填写控制端地址。
