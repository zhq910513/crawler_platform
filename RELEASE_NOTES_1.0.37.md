# crawler_platform 1.0.38

- 优化前端导航切换性能：主布局业务页面启用 KeepAlive 缓存，避免每次切换导航都销毁并重建表格、抽屉和复杂表单。
- 登录后空闲时间预加载常用业务页面组件，降低首次点击导航时的异步 chunk 加载等待。
- 增加公司列表 60 秒轻量引用缓存，减少多个页面切换时重复请求基础公司数据；新增公司后自动失效缓存。
- 执行记录页适配 KeepAlive：按任务/执行查询参数变化时自动刷新，避免页面缓存后打开指定日志不更新。
- 项目管理页适配 KeepAlive：从其他页面带公司或打开接入弹窗参数进入时仍能响应。
- 增加前端导航性能契约测试，防止后续回退成全量销毁式导航。
