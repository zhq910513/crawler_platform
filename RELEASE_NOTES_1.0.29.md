# crawler_platform 1.0.29 发布说明

## 版本

- crawler_platform：1.0.28 → 1.0.29

## 本次重点

1.0.29 聚焦项目首次部署运维路线：平台已稳定运行、新爬虫项目已同步 Git、目标服务器首次部署项目时，尽量通过平台一键完成部署编排，减少人工登录服务器操作。

## 核心改动

1. 新增项目一键部署状态机 V1。
   - 发布源确认。
   - release 契约校验。
   - 服务器 Agent 预检。
   - Agent 部署指令下发。
   - Agent 拉取镜像、准备项目目录、执行运行时自检。

2. 新增 Agent 通用运维指令 V1。
   - 复用 `crawler_server.metrics` 存放轻量 pending/result 指令。
   - 不新增数据库表，降低生产迁移风险。
   - 指令具备稳定 `commandId`，便于幂等 ACK。

3. 项目部署前置校验收紧。
   - 目标服务器必须属于同公司。
   - 服务器必须启用。
   - 服务器必须已接入 Agent。
   - Agent 必须已经心跳上线。
   - Docker 状态、Docker socket 权限、项目目录可写性出现明确异常时阻断部署。

4. Agent 增加项目部署准备能力。
   - 按 release digest 拉取镜像。
   - 创建项目运行目录、缓存目录、日志目录、profile 目录和临时目录。
   - 用项目镜像执行轻量运行时 import 自检。
   - 结果回报平台后，平台再放开该服务器的项目计划状态。

5. 前端项目页增加一键部署入口和部署历史展示。
   - 项目列表可直接触发一键部署。
   - 项目详情展示最近部署单、状态和当前步骤。
   - 一键部署默认自动选择可用执行节点。

## 上线注意

- 1.0.29 不新增数据库迁移文件。
- 首次部署目标服务器需要 Agent 已完成接入并至少上报一次心跳。
- 前端源码包内不包含 `node_modules`，构建前仍需在目标环境安装依赖。
