# crawler_platform 1.0.50 执行节点清理与重新接入修复

## 修复

- 执行节点列表新增“重新接入”操作，异常或未上线节点可以直接重新生成接入命令。
- 执行节点列表新增“清理”操作，用于清理不健康、半接入或废弃节点记录，并释放节点标识和 Agent 编码。
- 后端新增 `DELETE /api/v1/servers/{server_id}`，清理前会阻断仍存在运行中任务的节点，避免误删正在执行任务的节点。
- 清理节点时同步解除项目节点关系、部署目标关系、离线快照、Agent 令牌和历史运行记录中的节点引用，避免唯一约束阻止重新接入。
- 节点列表返回 Agent 编码、Agent 状态、Agent 版本、Agent 最近异常，前端重新接入时会复用真实接入信息。

## 兼容性

- 不新增数据库表，不修改已有字段。
- 保留 `crawler_server.server_code` 与 `crawler_agent.agent_code` 唯一约束。
- 历史运行记录不删除，仅清空已清理节点/Agent 的引用。
