# Release Notes v1.0.99

## 修复内容

- 修复 `test_rebuild_contract.py` 中 Agent 接入脚本测试硬编码旧版本号 `1.0.97`，导致平台升级到后续版本后 CI 失败的问题。
- 将该断言改为读取仓库根目录 `VERSION` 的当前发布版本，避免后续每次升版都需要手工修改测试断言。
- 版本默认值统一提升到 `1.0.99`，不改动构建中心运行逻辑、数据库迁移或 Agent 接入业务契约。

## 影响范围

本版本仅修复 CI 版本契约残留问题，不改变项目发布、异步构建、Docker Engine API 兜底、Build Job 取消/重试/恢复等 v1.0.98 已有能力。
