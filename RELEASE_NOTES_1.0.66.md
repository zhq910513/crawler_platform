# 1.0.68 平台自检动作能力口径与节点验证引导专项

- 区分 CI/CD 自动处理、页面一键处理、平台服务器兜底、执行节点验证和云控制台处理。
- 只有页面白名单动作真实可用且存在必须处理项时，运行总览才展示一键处理按钮。
- 只有需确认项时，下一步动作改为执行节点验证，不再误导为继续自动准备执行组件镜像。
- 平台自检兜底命令统一为包含项目目录的完整单行命令。
- 页面自动检测快照在无状态变化时不再重复写入历史。
- 修复运行总览重复函数声明和前端必须处理文案遗留。
