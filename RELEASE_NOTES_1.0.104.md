# v1.0.104

## 修复

- 远程自动部署不再因为部署目录下的 `data/`、`.release/`、`logs/`、`tmp/` 等本地运行目录被 Git 识别为未跟踪文件而停止。真实源码改动、删除、未知未跟踪文件仍然阻断部署。
- 构建中心源码获取链路继续增强：`git` 命令缺失或 `git clone` 网络失败时，会依次尝试本地源码包、GitHub 官方归档包、上次成功源码缓存。
- 构建中心会在成功获取有效源码并读取版本后刷新源码缓存，后续 GitHub 出口异常时可用缓存兜底构建。
- CI 中的 Git clone retry 测试不再隐式依赖工具容器已安装 `git`。

## 边界

- 平台仍不会内置不可信第三方 GitHub 代理。若 `github.com/codeload.github.com` 长期不可达，推荐使用平台可访问的 Git 镜像仓库，或在 `CRAWLER_PROJECT_SOURCE_BUNDLE_DIR` 预置源码包。
- 本地源码缓存只能保证有过一次成功源码输入后的后续兜底，不能凭空获得从未成功获取过的仓库源码。
