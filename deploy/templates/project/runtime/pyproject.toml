[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "crawler-runtime"
version = "1.0.0"
description = "统一的爬虫容器任务方法执行器"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["crawler_runtime*"]
