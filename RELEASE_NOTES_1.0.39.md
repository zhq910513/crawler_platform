# crawler_platform 1.0.39

## 发布助手流水线强化

- 项目发布页保留发布助手作为主体验。
- 新增后端权威发布流水线检查接口，前端不再自行拼接关键状态。
- 发布必须按公司、服务器、代码仓库、构建镜像、确认版本、部署服务器、运行前自检顺序推进。
- 任一环节缺配置、状态异常或未满足前置条件时立即阻断，不能跳过继续。
- 平台构建中心未配置时明确卡在构建镜像步骤，不伪造自动构建成功。

## 验收重点

- 已有外部构建版本可继续通过发布助手部署。
- 未登记版本且构建中心未就绪时，发布助手必须给出阻断项。
- 多服务器部署仍复用既有 Agent 拉 digest 镜像和运行前自检链路。
