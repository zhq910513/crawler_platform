# crawler_platform 1.0.26 发布说明

## 版本递增

- crawler_platform：1.0.25 → 1.0.26
- crawler_platform_spiders：本轮未改，继续保持 1.0.13

## 本轮定位

1.0.26 聚焦“公司级权限隔离与前端访问收敛”。核心目标是：除超级管理员外，其他公司员工账号进入前端后，只能查看、配置和操作自己所属公司的资源，且后端不相信前端传入的 companyId。

## 核心整改

1. 后端权限模型收敛为两层：超级管理员全局管理，普通用户公司内管理。
2. 普通用户访问公司数据时，后端强制绑定当前登录用户的 company_id。
3. 普通用户手工篡改 companyId 访问其他公司资源时，统一返回“资源不存在”，避免泄露其他公司存在性。
4. 公司资源配置、执行节点接入、Agent join token、项目列表、Release 列表、任务列表、执行记录等接口按公司隔离。
5. 普通用户允许在自己公司范围内接入执行节点、生成 Agent 接入命令、接入本公司项目、配置本公司项目执行节点。
6. 前端隐藏普通用户的全局公司切换器，仅显示“当前公司”。
7. 执行节点页面普通用户可使用“接入执行节点”，但公司字段锁定为所属公司。
8. 项目管理页面普通用户可接入和部署自己公司的项目，但不能切换到其他公司。
9. 平台账号页面普通用户公司筛选锁定为自己公司。
10. 配置助手针对普通用户不会引导到全局系统设置；平台访问地址缺失时提示联系超级管理员。
11. 新增越权访问回归测试，覆盖普通用户跨公司写入配置、跨公司生成 Agent 接入凭证等场景。

## 安全边界

- 前端隐藏不等于权限控制，所有公司级接口均以后端当前登录用户为准。
- 普通用户提交其他公司的 companyId，不会被信任。
- 普通用户只能看到自己公司的公司信息、资源配置、执行节点、项目、账号、任务和执行记录。
- 超级管理员仍保留全局公司管理、用户管理、系统设置、操作日志等能力。

## 验证

- 后端回归测试通过：58 passed
- Python compileall 通过
- 版本号统一更新到 1.0.26
