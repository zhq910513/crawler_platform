# crawler_platform 1.0.68

## 平台自检控制端视角纠偏

- 运行总览平台自检回归“平台服务器 / 控制端服务器”视角，不再在运行总览展示执行节点验证脚本。
- 需确认状态改为提示超管确认云安全组、防火墙、平台入口端口和内置镜像仓库访问策略。
- 后端自检新增 `securityGroupChecklist`，统一返回平台服务器安全组 / 防火墙规则清单。
- 平台镜像仓库确认项改名为“平台镜像仓库公网访问”，强调 5000/TCP 应仅对执行节点来源 IP 放行。
- 内置镜像仓库安全项改名为“内置镜像仓库安全策略”，继续提示未认证 / 未 TLS 的内置 registry 不应全网开放。
- 执行节点完整预检脚本移动到执行节点接入流程，覆盖 `/health`、安装脚本、registry `/v2/` 和 `docker pull`。
- 更新发布契约检查，防止运行总览再次展示执行节点验证脚本，要求运行总览展示安全组规则清单。
