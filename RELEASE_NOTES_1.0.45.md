# crawler_platform 1.0.45

## 导航与执行节点文案收敛

- 将用户侧节点入口统一命名为“执行节点”。
- 移除前端展示中的内部容量口径，不再把系统内部字段暴露给操作员。
- 节点页面保留用户可理解的在线状态、健康状态、资源使用、运行中任务、Docker 检查、心跳和最近异常。
- 项目发布页将部署目标统一命名为“部署节点”，保留轻量选择和本页新增入口。
- 项目版本、任务编排、运行中心、执行记录中的节点相关文案统一改为“执行节点 / 部署节点 / 分配状态”。
- 后端接口字段和数据库字段不做破坏性改名，避免影响 Agent 心跳、任务分配、任务运行和历史数据兼容。
