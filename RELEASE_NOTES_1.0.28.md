# crawler_platform 1.0.28 发布说明

## 版本升级

- crawler_platform：1.0.27 → 1.0.28

## 本次重点

1. 删除任务时，平台会按项目 / 任务 Docker 标签向相关执行服务器下发容器清理指令。
2. 删除项目时，平台会按项目 Docker 标签向项目所在执行服务器下发容器清理指令；存在历史运行记录的项目归档隐藏并停用任务 / 调度。
3. Agent 心跳响应新增待清理容器指令，Agent 执行 stop/remove 后回报结果，平台确认后清空待处理队列。
4. 首次登录强制改密链路升级为后端硬状态机：未改密账号只允许改密、查询当前会话、上报当前会话活跃和退出。
5. 前端强制改密弹窗增加表单内错误提示、密码规则提示、本地校验，并阻止业务页面在改密前渲染。
6. 新增用户密码创建改为复用统一强密码规则，避免创建、重置、本人改密三处策略不一致。

## 兼容说明

- 未新增数据库表或字段。
- 容器清理指令暂存在 `crawler_server.metrics.pendingContainerCleanups` 中，由对应 Agent 心跳拉取执行。
- 清理容器依赖现有运行容器标签：`crawler.platform.project_id` 和 `crawler.platform.task_id`。
