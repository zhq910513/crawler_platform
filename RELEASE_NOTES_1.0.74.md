# crawler_platform v1.0.74 发布说明

v1.0.74 是 Agent 接入闭环修复版，针对 v1.0.73 现场验证暴露的问题继续修复。

## 核心修复

1. 平台生成接入命令前强制校验 Agent 目标版本与镜像 tag 一致，避免平台下发旧 Agent 镜像。
2. 安装命令不再包含 `exit $rc`，避免在交互式 SSH 中主动退出登录窗口。
3. 安装脚本不再把“容器启动成功”误报为“Agent 接入完成”，改为提示等待首次心跳。
4. Agent 独立版本升级到 `1.1.2`，平台版本升级到 `1.0.74`，协议版本仍为 `1.0`。

## 验证重点

- 安装命令不会退出用户当前 Shell。
- `AGENT_AGENT_VERSION=1.1.2` 时，`CRAWLER_AGENT_IMAGE` 必须指向 `:1.1.2`。
- 如果运行配置仍指向旧镜像，接入命令生成和 Bootstrap 会被阻断。
- 容器启动后需等待平台心跳确认，页面仍为准。
