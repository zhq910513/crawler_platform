# crawler_platform 1.0.12 发布说明

## 版本定位

本次版本：`1.0.12`

提交信息建议：

```text
优化任务调度面板间距并修复删除任务v1.0.12
```

## 修复内容

1. 任务调度面板视觉间距压缩
   - 筛选区从大字号、大间距调整为紧凑表单。
   - 筛选项改为 5 列紧凑排布，减少换行和纵向占用。
   - 表单标签、输入框、按钮高度统一压缩为 32px。
   - 高级筛选区域同步压缩间距。

2. 任务调度表格紧凑化
   - 表格字体由 16px 调整为 13px。
   - 表头高度由 78px 调整为 42px。
   - 数据行高度由 78px 调整为 46px。
   - 单元格默认不换行，长内容使用省略号和 tooltip。
   - 操作列宽度收窄，图标按钮间距压缩。

3. 修复删除任务没有成功的问题
   - 新增 `DELETE /api/v1/tasks/{taskId}`。
   - 没有历史执行记录的任务执行物理删除。
   - 有历史执行记录的任务执行归档隐藏，并自动停用调度，保留历史执行记录。
   - 存在运行中实例的任务禁止删除，避免破坏正在运行的任务。
   - 删除无历史任务后，会把对应任务定义恢复为可创建状态。

4. 任务调度面板默认隐藏归档任务
   - 默认列表不展示 `ARCHIVED` 任务。
   - 需要查看归档任务时，可通过任务状态筛选 `ARCHIVED` 查询。

5. 质量修复
   - 清理 `TaskUpdate` 中重复声明的 `cpu_limit` 字段。
   - 新增删除任务回归测试，覆盖物理删除、归档隐藏、归档筛选查询。

## 验证结果

```text
后端测试：41 passed
Python 编译检查：通过
压缩包完整性检查：通过
```

## 部署验证

上线后确认：

```bash
curl -s http://127.0.0.1:8080/version.json
curl -s http://127.0.0.1:8080/health
```

版本应为：

```text
1.0.12
```
