# crawler_platform 1.0.11 发布说明

## 版本定位

本版本在 1.0.10 基础上继续整改两类问题：

1. 修复 GitHub Actions 商业发布门禁中调度器重复触发测试受历史测试数据影响导致的随机失败。
2. 按用户提供的截图继续精修“任务调度”页面，使其更接近扁平任务运营后台样式。

提交信息建议：

```text
修复门禁并精修任务调度面板v1.0.11
```

## 修复内容

### 后端 / 测试

- 修复 `test_scheduler_duplicate_trigger_does_not_rollback_previous_schedule` 的测试隔离问题。
- 该测试现在会在用例内部停用非当前用例创建的历史调度，避免 `dispatch_due_schedules()` 全局扫描到其他已到期调度，使 `created` 计数在 CI 中受分钟边界影响。
- 保持生产调度器行为不变：生产环境仍会扫描并处理所有到期调度。

### 前端 / 任务调度面板

- 任务调度页改为更贴近截图的“任务运营后台”布局。
- 主筛选区调整为：任务名称、目标任务路径、任务表名、服务器分组、任务组名、任务平台、任务状态、开发人员、目标任务状态。
- 公司、项目、计划状态收进高级筛选，保留多租户和项目过滤能力但不抢占主视觉。
- 表格列顺序调整为：选择框、任务编号、服务器、任务平台、任务名称、上次任务完成时间、目标任务路径、cron执行表达式、下次执行时间、开发人员、状态、目标状态、操作。
- 状态列使用开关控制任务启停；目标状态使用标签展示最近运行结果。
- 工具栏补齐：新增、修改、删除、导出、日志，以及右侧搜索/刷新圆形按钮。
- 操作列改为图标化：编辑、删除、立即执行、查看、调度配置。
- 删除行为采用安全停用任务，不物理删除历史执行记录。
- 导出支持导出当前页或已选任务为 CSV。

## 验证结果

```text
后端测试：40 passed
Python 编译检查：通过
前端 Vue 文件结构检查：通过
版本号统一：1.0.11
```

## 上线前必须执行

```bash
bash deploy/scripts/commercial-release-gate.sh
```

必须看到：

```text
RELEASE_GATE=PASS
```
