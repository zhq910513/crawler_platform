# crawler_platform 1.0.35

## 变更

- 公司管理页新增“生成接入密钥”按钮。
- 超级管理员可在前端为公司生成新的爬虫项目接入密钥。
- 生成结果只显示一次，关闭后不能查看原文；后端仍只保存密钥摘要。
- 页面明确提示将密钥复制到 GitHub 仓库的 `CRAWLER_DISCOVERY_TOKEN`。
- 保持新控制端链路，不恢复旧的 `CRAWLER_PLATFORM_DISCOVERY_TOKEN`。

## 校验重点

- 后端生成密钥接口仍由超级管理员调用。
- 前端不展示旧密钥、不新增查看明文入口。
- 生成按钮、复制提示和一次性展示契约已纳入测试。
