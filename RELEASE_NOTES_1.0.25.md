# crawler_platform 1.0.25 发布说明

## 版本

- crawler_platform：1.0.24 → 1.0.25
- crawler_platform_spiders：本轮未改，保持 1.0.13

## 本轮定位

1.0.25 聚焦“配置助手 + 首次/再次部署识别 + UI 舒适性整改”。目标不是增加复杂流程引擎，而是在用户新增公司、继续配置、再次部署时，帮助用户快速判断已完成内容、缺失内容和下一步应该进入哪个界面。

## 主要变化

### 配置助手

- 新增右侧全局“配置助手”浮窗。
- 新增公司成功后自动打开配置助手。
- 公司列表操作列改为“继续配置”。
- 配置助手打开时会读取当前公司状态，不会每次从零开始引导。
- 自动区分：首次配置、继续配置、已有配置检查、基本完成。
- 配置助手每一步只做：说明、状态、跳转，不强制阻塞业务页面。

### 公司初始化闭环

配置助手按以下顺序展示：

1. 平台访问地址
2. 公司数据库
3. 执行节点
4. 爬虫项目
5. 采集平台
6. 平台账号
7. 任务调度

每一步展示“已完成 / 未配置 / 待处理 / 有风险 / 暂不可用”。

### 系统设置

- 系统设置新增“基础配置”。
- 支持保存“平台访问地址”。
- 执行节点接入命令优先读取系统配置的平台访问地址。
- 保留环境变量 PLATFORM_PUBLIC_URL 作为兜底来源。

### 公司管理

- 移除公司列表中的项目发布凭证按钮。
- 公司管理只保留公司基础信息和“继续配置”入口。
- 新增公司后不再让用户猜凭证用途。

### 数据库配置

- 新增“数据库配置”菜单和页面。
- 支持配置主业务数据库、登录缓存库、原始数据存储、媒体存储。
- 敏感字段在列表中脱敏展示。
- 提供基础测试/确认能力，配置助手据此判断数据库步骤是否完成。

### 采集平台

- 新增“采集平台”菜单和轻量检查页。
- 当前从项目任务定义和平台账号状态汇总采集平台准备情况。
- 页面明确说明采集平台是被采集的网站或系统，避免和“爬虫管理平台”混淆。

### 执行节点接入

- 执行节点接入向导继续保留远程服务器/本机测试区分。
- 生成接入命令时读取系统平台访问地址。
- 远程服务器仍会拦截本机地址。

## 后端接口

- GET /api/v1/system-settings
- PATCH /api/v1/system-settings
- GET /api/v1/companies/{companyId}/setup-status
- GET /api/v1/company-resource-configs
- POST /api/v1/company-resource-configs
- POST /api/v1/company-resource-configs/{configId}/tests

## 验证

- 后端回归测试：57 passed
- Python compileall：通过
- Shell bash -n：通过
- Alembic 迁移图检查：通过
- MySQL JSON/TEXT/BLOB 默认值检查：通过
- MySQL 标识符长度检查：通过
- 前端可见文案检查：通过
- 前端字典重复键检查：通过
- 执行节点接入合约检查：通过

当前沙箱缺少完整前端构建依赖，未声明 npm run build 通过；最终前端 Docker 构建仍以 CI/CD RELEASE_GATE=PASS 为准。
