# v1.0.72 Agent 生命周期与自动运维收口版

## 核心定位

v1.0.72 将 Agent 从“平台版本附件”调整为“服务器基础设施”。平台版本、Agent 版本、通信协议版本正式解耦：

- Platform：`1.0.72`
- Agent：`1.1.0`
- Protocol：`1.0`

平台普通 patch 发布不再要求所有执行节点重新拉 Agent 镜像、重新 Join 或手工 SSH。Agent 只有在自身代码、协议能力或最低兼容要求变化时才进入独立升级流程。

## 主要变化

### 1. Agent 版本独立

- `AGENT_AGENT_VERSION` 从平台 `APP_VERSION` 中解耦，当前独立版本为 `1.1.0`。
- `CRAWLER_AGENT_IMAGE` 默认跟随 Agent 版本，而不是平台版本。
- `protocolVersion` 统一为 `1.0`，修正历史数据库默认值与 Agent 实际上报不一致的问题。
- 发布脚本不再在普通平台升级时强制构建、推送或替换 Agent。

### 2. 首次接入流程重构

- Join Token 只作为一次性 Bootstrap 凭据。
- 安装器先做零副作用 Preflight，再消费 Token。
- Registry 网络检查、已有 Agent 检查、任务占用检查均前置。
- Token 已消费后，如果镜像、网络、容器启动阶段失败，后续可基于长期 Agent Credential 续跑，不再必须重新 Join。
- 正式安装命令改为“下载临时脚本 → 执行 → 保留退出码 → 清理临时文件”，不再使用 `curl | bash` 管道。

### 3. 已有 Agent 智能替换

- 默认启用智能替换，不再要求普通用户理解 `--replace-existing-agent`。
- 用户显式选择保守模式时才生成 `--no-replace-existing-agent`。
- 已有 Agent 正在运行任务时，安装器停止并保留 Token，不做替换。
- 替换过程具备回滚：先保留旧容器，再启动新容器；新容器最小存活验证失败时恢复旧容器。

### 4. 生命周期状态与历史证据

- 新增节点期望状态、生命周期状态、生命周期动作、前置管理状态等字段。
- 任务领取时固化执行节点快照，包括节点名、Agent 版本、协议版本、镜像和 digest，节点退役后仍可追溯历史运行证据。
- 节点移除改为 Desired State 驱动的退役流程，不再把“删除数据库记录”伪装成远端已清理。
- 离线或 legacy 节点移除后会撤销长期身份，并生成 P1 远端清理未确认告警。

### 5. 安装和诊断体验

- 所有用户可见网络检查命令增加硬超时。
- Docker HTTP Registry 配置移动到长期身份建立之后，避免 Token 无效但宿主机已被修改。
- Docker Registry 高风险配置仍需要显式授权，默认不自动修改。
- `docker pull` 保留原始错误，不再吞掉 stderr。

## 不在本版范围

- Registry HTTPS / 认证 / 镜像签名。
- Agent 批量灰度升级与复杂自动回滚策略。
- 云厂商安全组 API 自动修改。
- SSH 自动加固。

这些能力已通过版本、协议、期望状态和生命周期模型预留接口，但不在 v1.0.72 同时展开。

## 验收重点

- 平台 `1.0.72` 普通发布不触碰 Agent。
- Agent `1.1.0` 可独立管理。
- 已有 Agent 默认智能替换且有回滚。
- 有运行任务时不强杀 Agent。
- Join Token 不在安全拦截场景中被浪费。
- 节点移除后当前列表清理，历史运行证据保留。
- 远端清理未确认不会被隐藏。
