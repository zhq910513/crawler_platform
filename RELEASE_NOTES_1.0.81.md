# crawler_platform v1.0.81 发布说明

v1.0.81 是在 v1.0.80 CI 门禁修复版基础上的执行节点接入体验修复版，重点处理新增执行节点弹窗生成命令后的交互问题和首次心跳状态刷新问题。

## 变更内容

1. 生成接入命令后，不再弹出后端 warnings 的额外提示；只保留阻断型地址错误提示，避免用户点击“生成接入命令”后被非阻断提醒打断。
2. 生成接入命令后自动滚动定位到命令区域，适配“新增执行节点”和“项目发布页内新增执行节点”两个入口。
3. 生成命令后自动轮询节点/接入状态；节点首次心跳成功后自动更新步骤状态，并在项目发布页自动选中已上线节点。
4. 调整安装脚本成功提示文案，明确控制台会自动刷新首轮心跳状态；如果 30 秒后仍显示接入中，再查看 `docker logs --tail 200 crawler-agent`。
5. 新增前端接入交互契约测试，防止后续回退到生成命令后不定位、不刷新或继续弹出 warnings 的旧行为。
6. 平台版本统一提升到 `1.0.81`。

## 验证结果

- Python 语法编译通过。
- 发布包卫生、前端接入交互契约、前端资源页契约、运行绑定注入和重建契约测试通过，共 `75 passed`。
- 版本一致性检查通过：`releaseVersion=1.0.81`，`warnings=0`。

## 注意事项

本次修复解决控制台刷新与交互问题，不伪装成已经确认目标机 Agent 真实心跳成功。若目标机仍显示“接入中”，请优先查看目标机：

```bash
docker logs --tail 200 crawler-agent
docker inspect -f '{{.Config.Image}}' crawler-agent
```

如果目标机实际运行的 Agent 镜像仍是旧版本，例如 `crawler_platform_agent:1.0.71`，需要同步生产 `.env` 中的 `AGENT_AGENT_VERSION` / `CRAWLER_AGENT_IMAGE`，并重新执行 Agent 镜像准备与节点接入。
