# 1.0.62 平台接入成熟化专项

本版本承接 1.0.59-1.0.62 的连续成熟化路线：平台自检快照持久化、执行节点接入邀请状态、Agent 镜像 digest 校验、内置 registry 安全增强。

## 核心变化

- 1.0.59：新增平台自检快照持久化，运行总览展示最近检测记录和状态变化。
- 1.0.60：接入令牌增加待接入/接入中/已接入/接入失败等状态，减少失败节点污染正式列表的排障成本。
- 1.0.61：Agent 镜像增加 digest 读取、记录和心跳上报，避免只看 tag 导致版本内容不可确认。
- 1.0.62：内置 registry 增加 HTTP/认证/TLS 安全姿态检查，明确提示仅允许执行节点 IP 访问 5000/TCP。

## 自动化边界

- 平台可自动完成的检测、快照、digest 读取、状态变化计算已代码化。
- 修改云安全组、开启云 registry 认证等外部系统动作仍标记为人工处理或云控制台处理。
- 高风险宿主机动作继续通过白名单动作或安装脚本授权参数执行，不开放任意 shell。
