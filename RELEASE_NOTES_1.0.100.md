# v1.0.100

## 变更摘要

- 修复平台构建中心拉取 GitHub 源码时遇到 TLS/网络瞬断后直接失败的问题。
- 源码拉取改为可配置重试，默认 3 次，并强制 Git HTTP/1.1、低速超时和非交互模式。
- 构建日志中记录每次 clone 尝试、重试等待和最终失败上下文，便于在发布页直接判断是网络问题、凭据问题还是仓库不可访问。

## 风险与边界

- 该版本不改变构建中心的 Docker build/push/Release 登记逻辑。
- 如果 GitHub 长期不可达，重试只能缓解瞬时故障，仍建议将爬虫项目同步到控制端访问稳定的 Git 镜像仓库。
