# crawler_platform 1.0.46 平台访问入口统一专项

## 变更目标

统一平台公网访问入口、健康检查地址和执行节点接入命令生成口径，避免平台实际运行在 8080 时生成 80 端口命令，或默认部署入口与安全组/Nginx 暴露端口不一致。

## 关键改动

- 新部署默认 Web 入口改为 80：`WEB_PORT=80`，`docker-compose.yml` 默认映射 `${WEB_PORT:-80}:80`。
- `.env.example` 不再默认写死 `CRAWLER_CONTROL_PUBLIC_BASE_URL=http://127.0.0.1:8080`，留空时由当前请求入口自动识别。
- Web Nginx 反代保留外部 `Host` 端口：使用 `$http_host` 和 `X-Forwarded-Host`，避免后端丢失 `:8080`。
- 后端控制端地址解析增加同主机端口纠偏：配置为裸 IP，但实际请求来自 `IP:8080` 时，优先使用真实请求入口生成接入命令。
- Agent 接入命令与连通性验证命令始终使用同一个控制端入口。
- 部署、测试和烟测脚本默认健康检查入口同步到 80。

## 兼容说明

- 生产推荐公网入口为 `http://公网IP/health` 或 `https://域名/health`。
- 如果临时继续使用 8080，必须同时设置：`WEB_PORT=8080` 与 `CRAWLER_CONTROL_PUBLIC_BASE_URL=http://公网IP:8080`，或确保通过浏览器 `:8080` 入口生成接入命令。
- 后端接口字段和数据库结构未改变。
