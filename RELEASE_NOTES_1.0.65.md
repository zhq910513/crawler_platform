# 1.0.65 导航栏版本展示、运行总览 UI 与 CI/CD 执行组件镜像自动化专项

## 核心目标

- 保持旧版深色侧边栏风格、宽度、字体、菜单密度与选中态不变。
- 修复导航项增多后左下角当前版本不可见的问题。
- 优化运行总览平台自检文案，避免“阻断阻断 1”“平台脚本可处理 1”等内部化表达。
- 在 GitHub Actions CI/CD 场景下自动传入平台服务器公网主机，并强制执行组件镜像准备成功，避免部署成功后运行总览仍残留执行组件镜像地址阻断项。

## 已完成

1. 侧边栏固定 100vh，菜单区域内部滚动，左下角版本卡固定保留。
2. 左下角版本卡显示当前版本与服务端版本，版本不一致时提示重新部署。
3. 运行总览自检状态从重复的“阻断阻断 1”收敛为“必须处理 1 / 需确认 0”。
4. “平台脚本可处理”改为“平台可一键处理 N 项”。
5. 下一步动作卡片按“问题 / 影响 / 推荐动作 / 一键处理 / 兜底命令”展示。
6. GitHub Actions SSH 入口传入 `CP_DEPLOY_PUBLIC_HOST=${SERVER_HOST}` 和 `STRICT_AGENT_IMAGE_PREPARE=1`。
7. `prepare-agent-image.sh` 支持从 `CP_DEPLOY_PUBLIC_HOST` 兜底推导执行组件镜像仓库公网主机。
8. 新增契约检查，防止再次缺失 CI/CD 公网主机注入、强门禁以及未产品化自检文案。

## 影响说明

- CI/CD 标准部署场景下，不再要求操作员登录平台服务器手工执行 `prepare-agent-image.sh` 来写入执行组件镜像公网地址。
- 如果执行组件镜像自动准备失败，CI/CD 会失败并暴露具体日志，不再出现“平台部署成功但运行总览仍显示镜像地址阻断”的半完成状态。
- 云安全组 / 防火墙放行 5000/TCP 仍属于云控制台权限边界，当前需要人工或未来接入云 API 后授权自动化。
