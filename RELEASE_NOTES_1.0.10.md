# crawler_platform 1.0.10 发布说明

## 版本定位

本版本是 1.0.9 任务调度面板整改后的上线前最高规格自检修复补丁。

提交信息建议：

```text
上线前最高规格自检修复v1.0.10
```

## 修复内容

- 修复固定镜像版本可以跨项目绑定的风险：`PINNED` 策略下的 `fixed_release_id` 必须属于当前任务所在项目和公司，并且发布状态必须可用。
- 加固运行实例解析发布版本逻辑：运行前再次校验发布版本归属，避免任务配置被绕过后运行错误镜像。
- 修复历史脏数据下 `PINNED` 策略缺少 `fixed_release_id` 时会回退到发布通道的问题，缺失固定版本时现在会拒绝运行。
- 修复 Agent 领取任务的并发竞态：领取时使用条件更新将 `QUEUED/ROUTED` 原子变更为 `ASSIGNED`，避免多 Agent 同时领取同一个运行实例。
- 修复调度批处理回滚风险：单个调度触发失败或唯一键冲突时，不再回滚同批次已成功创建的运行实例。
- 修复重复调度触发键导致 nextRunAt 卡住的问题：检测到重复触发后会推进该调度的下一次执行时间。
- 修复分片任务在任务调度面板中最近结果可能显示子分片而非父聚合运行的问题。
- 新增 `crawler_task_run(task_id, parent_run_id, run_id)` 查询索引，支撑面板按顶层运行获取最近结果。
- 新增 3 个回归测试用例，覆盖固定镜像越权、固定镜像缺失、调度重复触发隔离、分片父运行展示等关键风险路径。

## 验证结果

```text
后端测试：40 passed
Alembic head：0005_110_audit
```

## 上线前验证

```bash
bash deploy/scripts/commercial-release-gate.sh
```

必须看到：

```text
RELEASE_GATE=PASS
```
