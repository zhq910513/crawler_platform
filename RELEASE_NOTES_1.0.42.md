# crawler_platform 1.0.42

## 项目发布页 UI 收敛专项

- 删除页面内重复的大标题卡片，保留顶部导航标题作为唯一页面标题。
- 删除中间“当前准备度”整块卡片，将准备摘要收敛到右侧发布助手。
- 发布页恢复为左侧发布表单、右侧发布助手的两区结构，避免三列挤压。
- 发布助手继续作为唯一流水线状态源，未完成前只能收起为小浮窗，完成后才允许关闭。
- 统一发布表单 label、输入框、右侧新增按钮和底部按钮区对齐。
- 将“重置”按钮移到底部操作区，和“检查流水线 / 发布项目”保持一致。
- 新增服务器抽屉继续不展示控制端公网回调地址，仅提示由系统设置统一管理。

## 契约说明

- 本次不修改后端发布状态机、不修改 API 入参与返回结构。
- 本次不新增数据库字段、不新增后端配置项、不调整 Agent 接入契约。
