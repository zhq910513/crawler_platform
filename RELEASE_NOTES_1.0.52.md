# crawler_platform 1.0.52 控制端预检强阻断与审查修复

本版本在 1.0.51 控制端对外连通预检基础上继续收敛执行节点接入链路。

## 修复

- 后端生成远程执行节点接入命令前强制执行控制端接入预检，避免直接调用 API 绕过前端阻断。
- 远程接入预检未通过时直接返回 40075，提示阻断项和修复建议。
- 接入命令返回体补充 `controlPlanePreflight`，便于前端和自动化流程复用同一套预检结果。
- 增强执行节点接入合约扫描，防止后续版本再次只做前端预检而漏掉后端强阻断。

## 校验重点

- 控制端公网地址、安装脚本地址、Agent 镜像仓库和必要端口仍由同一套预检结果展示。
- 本机测试接入仍允许使用本机地址；远程接入必须满足控制端对外连通条件。
