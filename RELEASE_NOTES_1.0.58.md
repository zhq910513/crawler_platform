# crawler_platform 1.0.58 平台自检一键动作与审计专项

## 核心修复

- 运行总览平台自检新增“自动准备 Agent 镜像”受控动作入口。
- 后端新增白名单平台动作接口，不暴露任意 shell 命令。
- 平台动作具备执行锁、阶段日志、失败阶段、手动兜底命令和操作审计。
- 未启用宿主机执行权限时，不再伪装成平台可一键处理，而是返回明确原因与兜底命令。
- 新增节点命令不再默认携带 `--replace-existing-agent`；只有重新接入场景才显式授权替换已有 Agent 容器。
- 平台自检项返回 `actionEndpoint/actionButtonLabel`，前端能区分按钮动作与手动兜底命令。

## 自动化边界

- 可代码自动化完成：Agent 镜像准备、状态展示、执行锁、阶段日志、审计、失败兜底提示。
- 需要授权后自动化：重启后端服务、替换已有 Agent、配置执行节点 Docker HTTP registry。
- 仍需人工处理：云安全组、云防火墙、域名和证书等平台暂无授权的外部系统操作。
