# crawler_platform 1.0.40

## 核心变更

- 发布助手继续采用强流水线模式，前端展示流程状态，后端做权威阻断判断。
- 修复普通账号首次登录强制改密后的重登体验：改密成功后使用明确的重新登录标记，抑制预期内旧会话失效提示，重登后默认进入项目发布主流程。
- 收紧用户接口输出，创建、更新、列表不再返回 passwordHash，避免密码摘要泄露到前端和操作员页面。
- 补充完整账号链路契约：超级管理员创建普通账号、首次登录、强制改密、旧 token 失效、旧密码不可用、新密码重登、业务接口恢复可访问。

## 验收重点

- 普通账号首次登录必须被强制改密，未改密前不能访问业务接口。
- 改密成功后旧 token 必须失效，使用新密码重新登录后不再要求改密。
- 用户接口响应中不得出现 passwordHash / password_hash。
- 项目发布流水线遇到缺失配置必须阻断，不能继续下游部署。
