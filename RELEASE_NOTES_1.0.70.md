# crawler_platform 1.0.70

## 运行总览极简信息层级

- 运行总览首页重新收敛为“核心指标 + 平台状态 + 当前异常”，正常状态不再展开检测过程。
- 核心指标统一简化为项目、执行节点、任务、运行中、等待资源，减少重复“数量”等弱信息。
- 平台状态卡只展示当前结论、已验证数、待自动验证数和异常数；检测来源、时间、证据和逐项结果下沉到详情。
- 当前无异常时仅保留一行“暂无需要处理的问题”；只有存在真实 FAIL/WARN 时才展开异常列表。
- 待自动验证项不再在首页逐项铺开，继续保持不等于异常、不产生人工待办的 v1.0.69 契约。
- 自动检测历史从首页移入“平台状态详情”抽屉，避免旧版历史记录干扰当前运行判断。
- 安全建议、网络安全建议和接入端口统一下沉到“安全与接入”页签，不参与首页运行健康表达。
- 详情抽屉按“检测项 / 安全与接入 / 检测历史”分层，保留重新检测、受控自动处理、验证命令和历史追溯能力。
- 导航副标题从“整体运行情况与待处理事项”改为“平台运行状态与自动检测”，彻底移除旧的管理员待办中心角色。
- 发布门禁同步更新，禁止运行总览首页重新出现检测历史、状态胶囊或待验证明细铺开。

## 验证

- 执行节点接入/运行总览契约门禁通过。
- 前端可见文案门禁通过。
- 后端测试：55 + 30 = 85 passed。
- Python compileall、全部 Shell `bash -n`、30 个前端 TS / Vue script 静态语法转译检查通过。
- 前端完整 `npm ci/npm run build` 未在当前容器完成：离线 npm 缓存缺少 `zrender-5.6.1.tgz`，因此不将静态检查冒充完整构建验证。
- 源码/ZIP 解压后逐文件 SHA256、权限和文件清单在交付前执行一致性校验。
