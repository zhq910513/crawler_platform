# crawler_platform 1.0.44

## 发布助手内容去重与状态合并专项

- 删除项目发布页发布助手内的公司、服务器、代码仓库、发布准备四块摘要区。
- 将本地表单状态、流水线阻断原因合并到 7 步发布流程内展示，避免助手内部信息重复。
- 发布助手顶部状态改为“状态 · 百分比”，收起浮窗继续沿用同一进度口径。
- 后端发布接口、数据库结构、发布状态机不变。

## 验证重点

- 发布助手只保留 7 步流水线作为唯一状态源。
- 浮窗、收起、拖拽和吸附交互继续保留。
- 新增服务器抽屉继续隐藏控制端公网回调地址。
