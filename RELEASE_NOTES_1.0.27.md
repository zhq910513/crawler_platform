# crawler_platform 1.0.27 发布说明

## 版本变化

- crawler_platform：1.0.26 → 1.0.27
- crawler_platform_spiders：保持 1.0.13，本轮未改

## 核心定位

1.0.27 聚焦“公司员工视角的运行中心”和“项目 / 任务 / 容器 / 执行节点四层状态拆分”。目标是让公司员工按自然心理路径使用系统：先看公司有哪些项目，再看项目下哪些任务，再看任务失败原因和处理建议。

## 产品侧整改

1. 新增“运行中心”页面，普通公司员工默认进入。
2. 运行中心按“公司 → 项目 → 任务 → 执行详情”组织信息。
3. 单任务项目自动弱化任务层级，在项目卡片上直接展示状态与操作。
4. 多任务项目支持展开查看子任务。
5. 点击任务打开右侧详情抽屉，减少页面跳转。
6. 任务详情抽屉包含：概览、日志、事件、容器、执行节点、历史记录。
7. 状态展示从技术码转成中文结论和操作建议。
8. 技术字段默认隐藏，仅在详情抽屉中分层展示。
9. 运行中心顶部增加公司级运行概览卡片。
10. 普通公司用户访问后台时不再默认进入项目管理，而是进入运行中心。

## 后端与 Agent 整改

1. 新增 `crawler_run_container_snapshot` 表，用于保存任务运行容器状态快照。
2. 新增 Agent 回传接口：`POST /api/v1/agent-container-snapshots`。
3. Agent 在容器创建、运行、超时、退出和清理阶段回传容器快照。
4. 新增 `GET /api/v1/running-center` 聚合接口，统一返回公司、项目、任务、最近运行、容器和执行节点状态。
5. 任务运行记录继续绑定启动时的 releaseId、imageDigest 和入口快照，新版本部署不会影响正在运行任务。
6. 运行中心状态聚合拆分为项目状态、任务状态、容器状态、服务器状态。
7. 容器 OOM、容器异常、节点异常、账号异常、数据库异常等状态有独立处理建议。

## 用户体验原则

- 公司员工只关心自己公司的项目与任务是否正常。
- 公司员工不需要默认理解 releaseId、imageDigest、TASK_DEFINITION、containerId 等技术字段。
- 异常展示必须给出“问题 + 原因 + 下一步”。
- 部署项目不会自动启动任务。
- 新版本只影响后续新任务，不打断正在运行任务。

## 验收重点

1. 公司用户登录后可看到“运行中心”。
2. 运行中心只显示当前公司数据。
3. 单任务项目展示为一条直接可操作的项目任务。
4. 多任务项目可展开查看任务列表。
5. 任务详情抽屉可查看日志、事件、容器、执行节点状态。
6. Agent 可上报容器快照。
7. 容器快照可在运行中心任务详情中展示。
8. 前后端版本统一为 1.0.27。
