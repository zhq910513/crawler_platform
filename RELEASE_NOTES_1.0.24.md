# crawler_platform 1.0.24 发布说明

## 版本变更

- crawler_platform：1.0.23 → 1.0.24
- crawler_platform_spiders：不变，仍为 1.0.13

## 重点修复

1. 修复执行节点接入命令默认生成 127.0.0.1:8000 的问题。
2. 接入执行节点流程新增“远程服务器 / 本机测试”场景区分。
3. 生成接入命令时支持传入执行节点可访问的平台地址。
4. 远程服务器接入时禁止使用 127.0.0.1 / localhost / 0.0.0.0。
5. 接入命令新增连通性验证命令，运维先验证再安装。
6. 公司页面“生成接入凭证”改为“生成项目发布凭证”，明确该凭证用于爬虫项目流水线发布，不用于执行节点接入。
7. 执行节点接入前端改为向导式交互，自动生成服务器编号和执行器编号。
8. 修复 /api/v1/agent-installers/linux.sh 在 API 镜像内依赖仓库 deploy 目录导致 500 的问题。
9. 新增执行节点接入合约门禁，防止安装脚本、地址识别和凭证文案回退。

## 验收重点

- 执行节点接入命令不能默认使用内部 API 端口。
- 远程接入必须要求填写可被目标服务器访问的平台地址。
- 公司项目发布凭证和执行节点接入凭证不能混淆。
- 安装脚本接口必须在 API 容器内稳定可读。
